# Values used by docker-compose.yml for the local PostgreSQL container.
# Copy to `.env` and adjust. Placeholder values only — never commit real secrets.
#
# Application configuration lives in backend/.env and frontend/.env.local;
# see the `.env.example` in each app directory.

POSTGRES_USER=careeriq
POSTGRES_PASSWORD=careeriq
POSTGRES_DB=careeriq
POSTGRES_PORT=5432
