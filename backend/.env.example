# Placeholders only — never commit real secrets.

APP_ENV=development
DEBUG=false
LOG_LEVEL=INFO

# Driver must be async-capable.
DATABASE_URL=postgresql+psycopg://careeriq:careeriq@localhost:5432/careeriq
DATABASE_ECHO=false

# JSON list.
CORS_ALLOW_ORIGINS=["http://localhost:3000"]

# Not wired up yet; listed so the shape is known in advance.
# JWT_SECRET=replace-with-a-long-random-value
# JWT_ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=replace-with-model-name
# EMBEDDING_MODEL=replace-with-embedding-model-name
